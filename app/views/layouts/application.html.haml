!!! 5
%html
%head
	%title= yield :title || "Bangarang"
	= stylesheet_link_tag "application"
	= javascript_include_tag "application"
	= csrf_meta_tags
%body
	%header#header
		= image_tag "logo.png", width: 700

	.center
		%nav#mainNav
			%ul
				- if current_admin
					%li
						= link_to 'Approve Users', '/approved'
					%li
						= link_to 'Logout', '/logout'
				- else
					%li
						= link_to 'Admin Login', '/login'

		.content
			= yield