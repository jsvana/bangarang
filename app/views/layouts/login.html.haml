!!! 5
%html
	%head
		%title= content_for?(:title) ? yield(:title) : "Bangarang"
		= stylesheet_link_tag "application"
		= stylesheet_link_tag "bootstrap"
		= javascript_include_tag "application"
		= csrf_meta_tags
	%body
		.content
			= yield
