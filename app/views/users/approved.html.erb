<h1>Unapproved Users</h1>

<% @users.each do |user| %>
	<%= div_for user do %>
		<p><%= user.username %></p>
		<%= link_to "Approve", set_approved_user_path(user), method: :put, remote: true %>
	<% end %>
<% end %>