.span3
	%h1 Manage Users

	- @users.each do |user|
		= div_for user do
			%p.pad
				= link_to user.username, edit_user_path(user)
				- if user.approved
					%span.right Approved
				- else
					= link_to "Approve", set_approved_user_path(user), class: 'btn right', id: "#{user.id}_link", method: :put, remote: true
	
	= link_to 'New User', new_user_path, class: 'btn'